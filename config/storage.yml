test:
  service: Disk
  root: <%= Rails.root.join("tmp/storage") %>

local:
  service: Disk
  root: <%= Rails.root.join("storage") %>
# config/storage.yml
cloudinary:
  service: Cloudinary
  folder: <%= Rails.env %>
